<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}Gestion d'événements{% endblock %}</title>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}{% endblock %}

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>
<body>
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('home.acceuil') }}">
                Acceuil
            </a>

            <div class="navbar-brand">|</div>

            <a class="navbar-brand" href="{{ path('evenement.list') }}">
                Événements à venir
            </a>

            <div class="navbar-brand">|</div>

            <a class="navbar-brand" href="{{ path('type.list') }}">
                Événements à venir par type
            </a>

            <div class="navbar-brand">|</div>

            <a class="navbar-brand" href="{{ path('evenement.expire') }}">
                Événéments passé
            </a>

            <div class="navbar-brand">|</div>

            <a class="navbar-brand" href="{{ path('type.expire') }}">
                Événéments passé par type
            </a>
        </div>

        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <div>
                        <a href="#" class="btn btn-default navbar-btn">Connexion</a>
                    </div>
                </li>
                <li>
                    <div>
                        <a href="{{ path('evenement.create') }}" class="btn btn-default navbar-btn">Création d'un événement</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    {% block body %}{% endblock %}
</div>
</body>
</html>

{% extends 'base.html.twig' %}

{% block body %}

    <H2>Modifier l'événement {{ evenement.titre }}</H2>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default">Enregistrer</button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}